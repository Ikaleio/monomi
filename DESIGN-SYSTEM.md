# DESIGN-SYSTEM.md

## 目的

Monomi 是一个服务状态监控 DEMO。设计系统服务于两类页面：

- 公开状态页：让访客快速判断服务是否正常，并查看历史状态和事件。
- 后台运行总览页：让管理员快速判断整体运行情况，并定位需要关注的项目。

设计方向是克制、安静、信息优先的 Apple 风格界面。页面使用大留白、明确的排版层级、少量容器和连续的非线性动画。视觉表现不能降低状态数据的可读性。

## 设计原则

1. **先说状态，再说细节。** 正常、异常、维护和更新时间必须比装饰更醒目。
2. **减少容器。** 只在需要分组、对比或承载操作时使用卡片。
3. **保持呼吸感。** 页面使用较大的 section 间距和卡片内边距。
4. **用数据形成视觉重点。** 绿色状态面板、可用率数字和 90 天历史条是主要视觉锚点。
5. **动效服务于阅读。** 动画表达进入、层级、状态和空间关系，不制造等待。
6. **默认可访问。** 颜色不是唯一状态信号，控件必须有名称，数据必须可键盘访问。

## 色彩系统

全站只使用以下五个基础颜色。透明度和 `color-mix()` 可以生成层级，但不得引入新的主色。

| 名称 | Hex | 用途 |
| --- | --- | --- |
| 雾白 | `#F7F8F6` | 页面背景、浅色模式基础表面 |
| 纯白 | `#FFFFFF` | 卡片、弹层、主要反差文字 |
| 墨黑 | `#171A18` | 主文字、深色模式背景、侧栏主色 |
| 运行绿 | `#16A06A` | 正常状态、主要按钮、成功率和品牌重点 |
| 事件橙红 | `#E85D45` | 故障、待处理、证书风险和异常状态 |

### 语义 token

所有页面样式必须通过 `app/app.css` 中的 token 或 Tailwind 语义类使用颜色。

- `background`：页面背景。
- `foreground`：主文字。
- `card`：卡片和主要内容面。
- `primary`：运行绿，表示正常和主要操作。
- `destructive`：事件橙红，表示故障、风险或需要处理。
- `muted`：低对比度表面，用于统计分组和次级内容。
- `muted-foreground`：次级文字。
- `border`：低对比度分隔线。
- `sidebar-*`：后台导航专用语义 token。

推荐写法：

```tsx
<div className="bg-background text-foreground">
  <Badge variant="success">运行正常</Badge>
  <span className="text-muted-foreground">更新于 16:42</span>
</div>
```

不要在页面组件中写 `bg-[#...]`、`text-[#...]`、`bg-white` 或 `text-black`。如果语义不存在，先更新 token 或组件 variant。

### 对比度

- 绿色背景必须使用 `text-primary-foreground`。
- 深色背景必须使用 `text-foreground` 或 `text-primary-foreground`，不能沿用浅色表面的低对比文字。
- 异常不能只使用颜色表达。必须同时显示文本、图标或状态标签。

## 字体系统

项目使用两个自托管字体包：

- 正文：`Noto Sans SC Variable`，通过 `font-sans` 使用。
- 标题：`Noto Serif SC Variable`，通过 `font-serif` 使用。

### 字体角色

| 角色 | Tailwind 类 | 规则 |
| --- | --- | --- |
| 页面标题 | `font-serif text-4xl` 或更大 | 用于页面主标题，保持短句和清晰换行 |
| 区域标题 | `font-serif text-2xl` / `text-3xl` | 用于 section 标题和卡片标题 |
| 正文 | `font-sans text-sm` 至 `text-base` | 使用 `leading-relaxed` 或等效行高 |
| 数据 | `font-sans tabular-nums` | 可用率、数量和时间使用等宽数字 |
| 辅助信息 | `text-sm text-muted-foreground` | 不小于 14px，不抢主层级 |

正文不使用装饰字体。全站不新增第三种字体。标题优先使用 `text-balance`，长段落优先使用 `text-pretty`。

## 布局系统

### 公开状态页

- 页面最大宽度：`max-w-6xl`。
- 移动端水平内边距：`px-5`。
- 桌面端水平内边距：`md:px-8`。
- 公开页使用窄内容轴和宽状态卡片。
- 监视器按纵向列表排列，每个卡片显示可用率和 90 天历史。
- 事件区域使用开放式时间线，不把每条事件嵌套在多层卡片中。

### 后台总览页

- 桌面端使用固定侧栏和内容区。
- 侧栏展开宽度约 248px，收起宽度约 76px。
- 移动端隐藏桌面侧栏，使用顶部菜单展开导航。
- 内容区最大宽度：`max-w-7xl`。
- 核心统计使用响应式网格：移动端单列或双列，桌面端四列。
- 全局状态面板使用绿色主背景，并在同一面板内呈现主指标和辅助指标。

### 间距和圆角

- 优先使用 Tailwind spacing scale，例如 `gap-4`、`gap-6`、`py-8`、`p-6`。
- 不使用 `space-x-*` 或 `space-y-*`。
- 卡片使用较大的圆角，避免尖锐边框。
- 不要在页面中继续叠加卡片。一个内容区通常只需要一层主要容器。
- 避免没有明确用途的绝对定位和浮动布局。

## 组件规则

### shadcn/ui

组件位于 `app/components/ui/`。页面优先使用这些组件：

- `Button`：操作、导航和图标按钮。
- `Badge`：正常、异常、维护和数量状态。
- `Card`：监视器、统计和摘要分组。
- `DropdownMenu`：语言、主题和退出入口。
- `Tooltip`：90 天历史单元的日期详情和收起导航提示。
- `Separator`：事件列表和摘要列表的分隔。
- `Empty`：事件和待关注项目为空时的状态。

按钮内的图标使用 `data-icon="inline-start"` 或 `data-icon="inline-end"`，不要重复添加尺寸类。图标按钮必须有 `aria-label`。

### 状态 Badge

- 正常：`<Badge variant="success">运行正常</Badge>`。
- 故障或风险：`<Badge variant="destructive">需要关注</Badge>`。
- 次级信息：`<Badge variant="secondary">已解决</Badge>`。
- 非激活或边界状态：`<Badge variant="outline">维护完成</Badge>`。

如果新增状态，先在 `app/components/ui/badge.tsx` 中定义语义 variant，再在页面使用。

### 90 天历史

`StatusHistory` 的每个单元必须满足：

- 使用按钮或等效可聚焦控件。
- 有描述性的 `aria-label`。
- 可以通过 Tooltip 显示日期、状态、可用率和检查次数。
- 使用绿色表示正常、橙红表示异常、低对比墨黑表示维护。
- 保持 90 条记录连续排列。
- 移动端允许水平滚动，不压缩到无法辨认。

## 动画语言

项目使用 Framer Motion。动效采用 Apple 风格的平滑、轻微、连续节奏。

### 允许的动效

- 初始进入：`opacity`、短距离 `y` 位移和轻微 blur。
- 列表进入：stagger children，依次出现。
- 卡片悬停：轻微上移，不改变布局尺寸。
- 状态单元：从较小的 `scaleY` 弹性展开。
- 主状态图标：低频率轻微缩放、旋转或呼吸。
- 侧栏：使用 spring 过渡展开和收起。
- 移动导航：淡入、上移和轻微缩放。

### 动效参数

- 进入动画使用 `ease: [0.16, 1, 0.3, 1]` 或相近的平滑曲线。
- Spring 使用中等 stiffness 和低 bounce。
- 动画通常不超过 0.8 秒。
- 重要内容不得等待异步数据或动画完成后才出现。
- 滚动进入动画必须只播放一次：`viewport={{ once: true }}`。

### 禁止的动效

- 不使用渐变作为主要视觉效果。
- 不使用持续旋转、强烈弹跳或快速闪烁。
- 不使用会造成内容跳动的布局动画。
- 不使用抽象 blob、发光球或装饰性伪数据。
- 不使用动画隐藏异常信息。

## 内容和数据展示

- 页面文案使用简体中文。
- 状态词固定使用：运行正常、性能下降、服务中断、计划维护。
- 事件必须展示标题、时间、状态和简要说明。
- 时间、百分比和检查次数必须有明确单位。
- Mock 数据必须保留正常、异常、维护、事件存在和空状态的可展示结构。
- 不增加“实时连接”“检测中”之类会暗示真实后端行为的文案。

## 可访问性

- 使用 `main`、`header`、`nav`、`section`、`article` 和 `footer` 表达页面结构。
- 每个区域使用 `aria-labelledby` 指向可见标题，或提供明确的 aria label。
- 所有图标装饰使用 `aria-hidden="true"`。
- 所有图标按钮、收起导航按钮和历史单元必须有可访问名称。
- 键盘焦点必须清晰可见。
- 颜色之外必须有文字或图标状态表达。
- 移动端导航展开后必须仍然可以访问所有导航项。

## 验证清单

完成设计或样式改动后，检查：

- [ ] 页面只使用本设计系统定义的五个基础颜色。
- [ ] 正文使用思源黑体，标题使用思源宋体。
- [ ] `/` 和 `/overview` 都能直接访问。
- [ ] 桌面端和移动端都没有水平溢出，历史条除外。
- [ ] 公开页显示多个监视器、可用率、90 天历史和事件。
- [ ] 历史单元可以键盘聚焦并显示详情。
- [ ] 后台导航在桌面端可收起，在移动端可展开。
- [ ] 正常、故障、维护和空状态结构仍然存在。
- [ ] 动画不会阻挡文本阅读。
- [ ] 运行 `bun run typecheck` 和 `bun run build` 均通过。
